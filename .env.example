# Copy to .env for local development. Railway injects these in production.

# Server
PORT=3001
NODE_ENV=development
# Public origin of the app, used to build the Google redirect URI.
# Local: the Vite dev server (it proxies /api to the server). Railway: https://<service>.up.railway.app
APP_URL=http://localhost:5174

# Database (docker-compose default, host port 5434)
DATABASE_URL=postgres://app:app@localhost:5434/app

# Google OAuth 2.0 client (https://console.cloud.google.com/apis/credentials)
# Authorized redirect URI: ${APP_URL}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Random 32+ char string. Generate: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-long-random-string
